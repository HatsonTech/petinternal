name = "petinternal-publish"
main = "worker.js"
compatibility_date = "2024-11-01"

# Non-secret variables (set secrets separately — see ADMIN-KURULUM.md):
[vars]
GH_REPO = "HatsonTech/petinternal"
GH_BRANCH = "main"
ALLOW_ORIGIN = "https://www.petinternal.com"

# Secrets (DO NOT put here — set with `wrangler secret put` or in the dashboard):
#   ADMIN_PASSWORD
#   GITHUB_TOKEN
